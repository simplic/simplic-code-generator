using using Simplic.Framework.UI;
using Simplic.Localization;
using Simplic.Studio.UI;
using System;

namespace ..NamespaceName..
{
    /// <summary>
    /// Represents a viewmodel for managing. <see cref="..ModelName.."/>
    /// </summary>
    public class ..Name.. : ExtendableViewModel, IWindowViewModel<..ModelName..>
    {
        private ..ModelName.. model;
        private readonly ILocalizationService localizationService;

        public ..Name..()
        {
            localizationService = CommonServiceLocator.ServiceLocator.Current.GetInstance<ILocalizationService>();
        }

        /// <summary>
        /// Initialize viewmodel instance.
        /// </summary>
        /// <param name="model"></param>
        public void Initialize(..ModelName.. model)
        {
            this.model = model;
        }

        /// <summary>
        /// Gets an instance of the actual <see cref="..ModelName.."/>
        /// </summary>
        public ..ModelName.. Model => model;

        /// <summary>
        /// Gets the actual window title.
        /// </summary>
        public string WindowTitle => "WindowName";
    }
}